[project]
name = "whisperpocket"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "sounddevice",
    "webrtcvad-wheels",
    "numpy",
    "lightning-whisper-mlx",
    "pocket-tts",
]
